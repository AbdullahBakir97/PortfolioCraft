{
  "name": "@portfoliocraft/renderers",
  "version": "0.2.0",
  "description": "Markdown, JSON Resume, PDF, and SVG renderers for PortfolioCraft.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"import('node:fs/promises').then(async fs => { const { cpSync } = await import('node:fs'); cpSync('src/templates', 'dist/templates', { recursive: true }); })\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@portfoliocraft/core": "workspace:*",
    "@react-pdf/renderer": "4.1.5",
    "@resvg/resvg-js": "2.6.2",
    "eta": "3.5.0",
    "react": "18.3.1",
    "satori": "0.12.2"
  },
  "devDependencies": {
    "@types/node": "20.17.10",
    "@types/react": "18.3.18",
    "typescript": "5.6.3",
    "vitest": "2.1.8"
  }
}
