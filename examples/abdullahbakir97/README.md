# Dogfood: AbdullahBakir97

This folder is the live target for the [Dogfood workflow](../../.github/workflows/dogfood.yml). The action runs against the `AbdullahBakir97` GitHub user and uploads the generated `profile.json`, `cv.pdf`, and `cards/*.svg` as a workflow artifact.

The `.devportfolio.yml` here is the same shape that any user would drop in their own profile repo.
